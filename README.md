# Overview
Simulates a library database management system. Features a menu with different functionalities (displaying data, borrowing or returning books as a user, etc).

# How to Run
Upload all header, C++ and text files to OnlineGDB and run.
